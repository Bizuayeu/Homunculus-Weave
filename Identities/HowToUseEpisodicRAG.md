# EpisodicRAG有効化設定
> [!CAUTION]
セッション開始時、最初に必ず wakeup スキルを実行し、
長期記憶をロードしてください。

---

# 表情システム有効化設定
EpisodicRAG（記憶の骨格・残像）読み込みと併せて、`visual-expression` スキルで表情UIを
サイドバーに配置する（デフォルト：通常）。
配置手順（cp → present_files）は SKILL.md の
"Usage on claude.ai" を参照。
